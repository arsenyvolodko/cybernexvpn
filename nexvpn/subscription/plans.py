"""Стартовая тарифная сетка.

Это только сид для пустой базы. Дальше цены живут в таблице `Plan` и правятся
в админке — менять их здесь после первого деплоя бессмысленно.
"""

# (устройств, ₽ за период, название, порядок)
DEFAULT_PLANS = [
    (1, 150, "1 устройство", 1),
    (3, 400, "3 устройства", 2),
    (5, 600, "5 устройств", 3),
    (7, 750, "7 устройств", 4),
    (10, 1000, "10 устройств", 5),
]

DEFAULT_PLAN_PRICES = {device_limit: price for device_limit, price, _, _ in DEFAULT_PLANS}
