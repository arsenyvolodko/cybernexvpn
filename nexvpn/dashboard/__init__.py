"""Дашборд: одна страница, с которой видно, что происходит с сервисом.

Живёт внутри Django намеренно. «Активный пользователь», «купил после пробного»,
«пользовался и не продлил» — это смысл моделей, а не строчки в таблицах, и
считать их сырым SQL мимо приложения значит завести вторую копию логики,
которая разойдётся с первой на ближайшем изменении моделей.

Всё здесь — только чтение. Ни одна вьюха не пишет в базу.
"""
