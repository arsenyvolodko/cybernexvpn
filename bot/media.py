"""Картинки, которые бот прикладывает к сообщениям.

Лежат в репозитории, а не в панели или на CDN: их две с половиной штуки, они
меняются вместе с текстами и должны попадать в образ тем же выкатом. `COPY . .`
в Dockerfile забирает их сам.
"""

from pathlib import Path

ASSETS = Path(__file__).resolve().parent / "assets"

# Скриншот приложения: стрелка на кнопку обновления пинга, подчёркнуты значения
# у туннелей. На него ссылается CONNECT_SUCCESS словами «см фото» — если файл
# переименовать, текст останется врать, поэтому имя менять только вместе с ним.
CONNECT_SUCCESS_PHOTO = ASSETS / "connect_success.jpg"

# Подписи к медиа в Telegram ограничены 1024 символами против 4096 у обычного
# сообщения. Проверяем перед отправкой и при переполнении откатываемся на текст.
CAPTION_LIMIT = 1024
